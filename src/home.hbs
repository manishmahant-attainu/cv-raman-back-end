<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body>
    <h1>Welcome to home</h1>
    {{#if isLoggedIn }}
        <h1>Hello {{name}}</h1>
    {{else}}
        <h1>SignUp/Login</h1>
    {{/if}}
    <ul>
        {{#each items}}
            <li>{{this}}</li>
        {{/each}}
    </ul>
</body>
</html>